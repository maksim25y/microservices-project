server:
  port: 8888

spring:
  application:
    name: github-config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO:https://github.com/maksim25y/properties-test.git}
          username: ${GIT_USERNAME:maksim25y}
          password: ${GIT_TOKEN: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}
          default-label: main
          basedir: /app/config
